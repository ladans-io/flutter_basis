name: flutter_basis
description: Start point package for Flutter applications
version: 1.0.5+11
homepage:

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter:

dependencies:
  flutter:
    sdk: flutter

  equatable:

  http:

  get_it:

  url_launcher:

  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  assets:
    - assets/